name: Docker Image CI

on:
  push:
    branches: [ "rebase-playlist-database" ]
  pull_request:
    branches: [ "rebase-playlist-database" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
